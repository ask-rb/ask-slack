# API keys for VCR recording
# Copy to .env and fill in real values, then run tests to re-record cassettes
# These keys are NOT required for CI — cassettes serve as the HTTP mock layer.
# SLACK_TOKEN=# your SLACK_TOKEN here
